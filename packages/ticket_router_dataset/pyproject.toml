[project]
name = "ticket-router-dataset"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "ticket-router-base"
]

[tool.uv.sources]
ticket-router-base = { path = "../ticket_router_base", editable = true }
