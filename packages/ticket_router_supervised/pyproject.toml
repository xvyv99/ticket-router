[project]
name = "ticket-router-supervised"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "ticket-router-base",
    "datasets>=4.8.4",
    "joblib>=1.5.3",
    "pandas>=3.0.2",
    "scikit-learn>=1.8.0",
    "torch>=2.11.0",
    "transformers[torch]>=5.5.4",
    "xgboost>=3.2.0",
]

[tool.uv.sources]
ticket-router-base = { path = "../ticket_router_base", editable = true }
